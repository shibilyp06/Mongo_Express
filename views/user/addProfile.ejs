
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile Update</title>
  <style>
/* public/styles/profileUpdate.css */
body {
  margin: 0;
  padding: 0;
  font-family: 'Arial', sans-serif;
  background: linear-gradient(to right, #3494E6, #EC6EAD);
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
}
.container {
  background: rgba(255, 255, 255, 0.9);
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
  width: 400px;
  text-align: center;
}
h1 {
  margin-bottom: 20px;
  color: #333;
}
form {
  display: flex;
  flex-direction: column;
}
label {
  margin-top: 10px;
  font-weight: bold;
}
input,
select {
  padding: 10px;
  margin-top: 5px;
  margin-bottom: 15px;
  border: 1px solid #ccc;
  border-radius: 5px;
}
button {
  padding: 10px;
  background-color: #FF5757;
  color: #fff;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}
/* Adjustments for better readability */
body {
  background: #3494E6; /* Darken the background gradient */
}
.container {
  background: rgba(255, 255, 255, 0.9);
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
  width: 400px;
  text-align: center;
  color: #333; /* Darken the text color */
}
form {
  background: #fff; /* Add a white background to the form */
  padding: 20px;
  border-radius: 8px;
}
  </style>
  <link rel="stylesheet" href="/styles/profileUpdate.css">
</head>
<body>
  <div class="container">
    <h1>Profile Update</h1>
    <form action="/addProfilePost" method="post" enctype="">
      <label for="username">Username:</label>
      <input type="text" id="username" value="<%= user.firstName %>" name="username" required>
      <label for="email">Email:</label>
      <input type="email" id="email" value="<%= user.email %>" name="email" required>
      <label for="phone">Phone Number:</label>
      <input type="tel" id="phone" name="phone"  placeholder="123-456-7890" required>
      <label for="gender">Gender:</label>
       <input type="text" name="gender" placeholder="gender">
      <label for="dob">Date of Birth:</label>
      <input type="text" id="dob" name="dob" required>

    <a href=""> <button   type="submit">Update Profile</button></a> 
    </form>
  </div>
</body>
</html>

















































